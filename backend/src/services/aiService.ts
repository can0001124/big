import axios from 'axios';
export async function analyzeAICommand(command: string) {
  // OpenAI API ile komut analiz örneği (örnek response)
  return { platform: "instagram", action: "like", postUrl: "...", accounts: ["hesap1"] };
}
